# Forecasting Playbook — Generation 3 (calibrated to TRAIN miss distribution)

Built on gen-2 by calibrating ONLY to the TRAIN miss distribution. No web research, no outside knowledge. Rules apply to question TEXT plus leak-proof structural fields (direction, ladder_rung, ladder_pos_bin, is_deadline, trivial_bound). Every change cites its generation and the calibration evidence that created it.

What gen 3 changed: After gen 2, all three populated types are well-calibrated on the mean (deadline_completion gap +0.01 n=50; default gap +0.011 n=31; threshold_above gap 0.0 n=101). The biggest remaining bias is within-type dispersion hidden by a flat mean. The only flat type with a usable smoothed posbin candidate is deadline_completion. Gen 3 makes exactly that one change.

Rules (human-readable):

R1 — Default (unchanged from gen 2). If no typed rule fires, predict p = 0.43. Origin gen-1 seed (was 0.50). Changed gen 2: default predicted 0.50 but TRAIN resolved 0.419 over n=31 (gap +0.081) to 0.43 (0.419 shrunk ~10% toward 0.50). Unchanged gen 3: TRAIN mean_predicted 0.43 vs empirical 0.419 (gap +0.011, n=31), well-calibrated; per-bin empirics degenerate ([0,1,0.55,0,0.25] over n=[5,1,20,1,4]) with null monotone_candidate, so no posbin split.

R2 — Deadline/completion skews by ladder position (changed in gen 3). If is_deadline (discrete event happens by/before a date: pass, launch, sign, confirm, strike, arrest, resign, IPO), use probability_by_posbin[ladder_pos_bin] = [0.515, 0.466, 0.417, 0.368, 0.319] (lowest to highest strike bin). Origin gen-1 seed (was 0.30). Changed gen 2: predicted 0.30 but TRAIN resolved 0.42 over n=50 (gap -0.12) to flat 0.43. Changed gen 3: flat 0.43 well-calibrated on mean (gap +0.01) but masks ladder-position dispersion: TRAIN by_type_posbin empirical [0.533,0.5,0.412,0,0.385] over n=[15,2,17,3,13] trends down across strike bins. Replaced flat probability with isotonic non-increasing monotone_candidate [0.515,0.466,0.417,0.368,0.319] (captures higher-strike to less likely YES, smooths noisy n=2/n=3 bins so it generalises; mean ~0.42 preserves the calibrated type mean).

R3a — Threshold above skews by ladder position (unchanged from gen 2). If direction == above and not a deadline, use probability_by_posbin[ladder_pos_bin] = [0.679, 0.538, 0.397, 0.256, 0.115] (lowest to highest strike bin). Origin gen-1 seed (was flat 0.40). Changed gen 2: flat 0.40 (TRAIN resolve 0.366, n=101) masked strike dispersion (low ~0.737, high ~0.143) to isotonic monotone_candidate [0.679,0.538,0.397,0.256,0.115] (n=[19,13,24,17,28]). Unchanged gen 3: TRAIN mean_predicted 0.366 vs empirical 0.366 (gap 0.0, n=101), well-calibrated.

R3b — Threshold below (unchanged from gen 1). If direction == below and not a deadline, predict p = 0.60. Origin gen-1 seed. Unchanged gen 2 and gen 3: no TRAIN calibration data for this type (absent from by_type), so no evidence to recalibrate.

Machine-readable contract: Classify each question into the FIRST matching type in classification_order, then assign probability. If the type has probability_by_posbin (5-element array) use probability_by_posbin[ladder_pos_bin]; else if probability_by_rung use probability_by_rung[ladder_rung]; else use scalar probability. Output per question: {id, type, probability, base_rate_used, key_evidence, what_would_change_my_mind}.

JSON contract (generation 3):

{ "generation": 3, "default_probability": 0.43, "classification_order": ["deadline_completion", "threshold_above", "threshold_below", "default"], "types": { "deadline_completion": { "definition": "is_deadline is true: a discrete event resolving by/before a date.", "probability_by_posbin": [0.515, 0.466, 0.417, 0.368, 0.319], "origin_gen": 1, "changed_gen": 3, "evidence": "gen 2: predicted 0.30 but TRAIN resolves 0.42 over n=50 (gap -0.12) to flat 0.43. gen 3: flat 0.43 calibrated on mean (gap +0.01) but masks ladder-position dispersion (by_type_posbin empirical [0.533,0.5,0.412,0,0.385], n=[15,2,17,3,13]) to isotonic monotone_candidate [0.515,0.466,0.417,0.368,0.319] (mean ~0.42 preserves calibrated mean; smooths noisy n=2/n=3 bins)." }, "threshold_above": { "definition": "direction == above and not a deadline.", "probability_by_posbin": [0.679, 0.538, 0.397, 0.256, 0.115], "origin_gen": 1, "changed_gen": 2, "evidence": "gen 2: flat 0.40 (TRAIN resolve 0.366, n=101) masked strike dispersion (low ~0.737, high ~0.143) to isotonic monotone_candidate [0.679,0.538,0.397,0.256,0.115] (n=[19,13,24,17,28]). gen 3 unchanged: gap 0.0 (n=101), well-calibrated." }, "threshold_below": { "definition": "direction == below and not a deadline.", "probability": 0.60, "origin_gen": 1, "evidence": "gen-1 seed prior. Unchanged gen 2 and gen 3: no TRAIN calibration data (absent from by_type), so no evidence to recalibrate." }, "default": { "definition": "catch-all: no other type matched.", "probability": 0.43, "origin_gen": 1, "changed_gen": 2, "evidence": "gen 2: predicted 0.50 but TRAIN resolves 0.419 over n=31 (gap +0.081) to 0.43. gen 3 unchanged: gap +0.011 (n=31), well-calibrated; per-bin empirics degenerate (counts of 1), null monotone_candidate, so no posbin split." } } }